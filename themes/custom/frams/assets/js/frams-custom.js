/*
 *  Custom JS
 *
 */

(function ($) {
  // Store our function as a property of Drupal.behaviors.
  Drupal.behaviors.starratings = {
    attach: function (context, settings) {
      // code here
    }
  };

}(jQuery));
